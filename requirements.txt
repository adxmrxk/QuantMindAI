# Full dependency set for the QuantMind platform.
# Preferred install:  pip install -e ".[dev]"
# (The API/dashboard alone need only the core block; the rest power the
#  training stack, Kafka streaming, Claude generation, and experiment tracking.)

# --- core (API/dashboard, regime, backtest, RAG, agents, vector DB) ---
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
hmmlearn>=0.3.2
yfinance>=0.2.40
pyarrow>=15.0
fastapi>=0.110
uvicorn[standard]>=0.29
joblib>=1.3
networkx>=3.0
langgraph>=0.2
qdrant-client>=1.9

# --- ml: forecaster, RL agent, GNN training ---
torch>=2.2
gymnasium>=0.29
stable-baselines3>=2.3

# --- optional backends ---
kafka-python>=2.0       # streaming (in-memory bus needs nothing)
anthropic>=0.39         # Claude-generated copilot answers
# mlflow>=2.10          # experiment tracking (optional)

# --- dev ---
pytest>=8.0
httpx>=0.27
